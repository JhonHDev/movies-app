import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const MoviesScreen = () => {
  return (
    <View>
      <Text style={{fontSize: 30}}>Movies Screen</Text>
    </View>
  );
};

export default MoviesScreen;

const styles = StyleSheet.create({});
